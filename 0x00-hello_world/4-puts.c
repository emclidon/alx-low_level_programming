#include <stdio.h>

/**
 * main - prints out a string
 * Return: return 0
 */
int main(void)
{
	char str_to_print[] = "\"Programming is like building a multilingual puzzle";

	puts(str_to_print);

	return (0);
}
